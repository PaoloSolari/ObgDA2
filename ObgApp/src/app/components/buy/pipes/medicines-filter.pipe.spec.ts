import { MedicinesFilterPipe } from './medicines-filter.pipe';

describe('MedicinesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MedicinesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
